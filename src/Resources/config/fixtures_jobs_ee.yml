jobs:
    fixtures_locale_csv:
        order: 10
        connector: Data fixtures
        alias:     fixtures_locale_csv
        label:     Locales data fixtures
        type:      fixtures
        configuration:
            filePath: locales.csv

    fixtures_locales_access:
        order: 11
        connector: Data fixtures
        alias:     locales_access
        label:     locales_access
        type:      fixtures

    fixtures_job_yml:
        order: 20
        connector: Data fixtures
        alias:     fixtures_job_yml
        label:     Jobs data fixtures
        type:      fixtures
        configuration:
            filePath: jobs.yml

    fixtures_xlsx_init_import:
        order: 25
        connector: Akeneo Excel Initialization
        alias:     excel_init_fixtures
        label:     Excel initialisation
        type:      fixtures
        configuration:
            filePath: init.xlsx
